# Build output / deps
/web/node_modules/

# Compiled binary (produced by `make build`)
/perchboard

# Local working notes and scratch — not part of the project
/claude/
/tmp/

# OS / editor cruft
.DS_Store
*.swp

# NOTE: web/dist is intentionally committed — it is embedded into the Go binary.
